<script>
        $(document).ready(function(){
    
            $('#btnVerde2').attr({
                id: 'btnAprovar',
                name: 'btnAprovar'
            }).
            html('Aprovar');
    
            $('#btnVermelho').attr({
                id: 'btnReprovar',
                name: 'btnReprovar'
            }).
            html('Reprovar');

            $('#btnLaranja').attr({
                id: 'btnVisualizar',
                name: 'bntVisualizar'
            }).
            html('Visualizar');
        });

    $('#btnVerde').attr({
        id: 'btnLogin',
        name: 'btnLogin'
    }).
    html('Login');
    
    $('#btnLogin').click(function(){

        var name = $('#name').val();
        var pass = $('#pass').val();

        if (name == 'undefined' ||  name == '' || name < 3) {
            alert('name');
            return false;
        }
        if (pass == 'undefined' || pass == '' || pass.length < 3) {
            alert('Defina uma Senha');
            return false;
        } 
    });

</script>

